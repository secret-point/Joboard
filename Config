package.HVHCandidateApplicationUI = {
    interfaces = (1.0);

    build-system = npm-pretty-much;
    build-tools = {
        1.0 = {
            NodeJS = 12.x;
            NpmPrettyMuch = 1.0;
        };
    };

    # Use runtime-dependencies for when you want to bring in additional
    # packages when deploying.
    # Use dependencies instead if you intend for these dependencies to
    # be exported to other packages that build against you.
    dependencies = {
        1.0 = {
            StencilReactComponents = 1.x;
            StencilReactThemeDefault = 1.x;
            KatalMetrics = 2.0;
            KatalMetricsDriverSushi = 1.0;
            KatalMetricsSellerCentral = 2.0;
            HVHCommonUILibrary = 1.0;
            KatalLogger = 1.0;
        };
    };

    runtime-dependencies = {
        1.0 = {
        };
    };

    test-dependencies = {
        1.0 = {
            NodeJS-Brazil = 1.0;
        }
    }
};
